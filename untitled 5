import pandas as pd
from sklearn.feature_extraction.text import CountVectorizer
from sklearn.linear_model import LogisticRegression
from sklearn.metrics import accuracy_score


text_data = [
    "free ticket win",
    "good morning",
    "earn money online",
    "how are you",
    "claim your prize",
    "nice to meet you"
]
labels = [1, 0, 1, 0, 1, 0]

data = pd.DataFrame({"msg": text_data, "label": labels})
print("Dataset used:")
print(data)


vector = CountVectorizer()
X = vector.fit_transform(data["msg"])
y = data["label"]

lr = LogisticRegression()
lr.fit(X, y)

test = ["win free ticket"]
test_vec = vector.transform(test)
prediction = lr.predict(test_vec)

print("\nTesting Message:", test[0])
if prediction[0] == 1:
    print("Prediction: spam message")
else:
    print("Prediction: normal message")
